name: CBB Backtest Analysis

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: 'Start date (YYYY-MM-DD)'
        required: false
        default: '2025-11-01'
      end_date:
        description: 'End date (YYYY-MM-DD)'
        required: false
        default: ''

jobs:
  backtest:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run backtest
      run: |
        if [ -z "${{ github.event.inputs.end_date }}" ]; then
          # No end date specified, use today
          python backtest_picks.py "${{ github.event.inputs.start_date }}"
        else
          # Both dates specified
          python backtest_picks.py "${{ github.event.inputs.start_date }}" "${{ github.event.inputs.end_date }}"
        fi
